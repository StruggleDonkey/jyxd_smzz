<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jyxd.web.dao.UserDao">

    <resultMap id="userMap" type="com.jyxd.web.data.User">
        <result column="id" jdbcType="VARCHAR" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="password" jdbcType="VARCHAR" property="passWord" />
        <result column="create_time" jdbcType="VARCHAR" property="createTime" />
    </resultMap>

    <select id="Sel" resultType="com.jyxd.web.data.User">
        select * from t_user where id = #{id}
    </select>

    <insert id="insert" parameterType="com.jyxd.web.data.User" >
        insert into t_user (id,name,password,create_time) values (#{id},#{name},#{password},#{createTime});
    </insert>

</mapper>
